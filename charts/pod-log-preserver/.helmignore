# Patterns to ignore when building packages.
.DS_Store
.git/
.gitignore
*.tmp
*.bak
*.swp
*.orig
.vscode/
.idea/
# helm-unittest fixtures — assert rendered content and schema rejections in CI;
# not part of the released chart package.
tests/
