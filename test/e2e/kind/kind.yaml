kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
# Pinned node image matches the kind version pinned in aqua.yaml (re-resolve
# from `strings $(aqua which kind) | grep kindest/node` when bumping kind).
nodes:
  - role: control-plane
    image: kindest/node:v1.36.1
